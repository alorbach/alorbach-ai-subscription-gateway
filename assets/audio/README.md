# Audio assets

## test-speech.wav

Real speech sample for audio transcription tests. Says "Hello, this is a test." Generated via text2wav (espeak-ng TTS).

**Regenerate:** From project root, run `npm run generate:test-speech` (requires `npm install`).
